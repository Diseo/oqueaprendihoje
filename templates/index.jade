extends layout

block content
  include author
  each article in articles
    article.article.intro
      header
        p.date
          span= moment(article.date).format('DD. MMMM YYYY')
        h2
          a(href=article.url)= article.title
      section.content
        !{ typogr(article.intro).typogrify() }
        if article.hasMore
          p.more
            a(href=article.url) ver mais

block prepend footer
  div.nav
    if prevPage
      a(href=prevPage.url) « Novos
    else
      a(href='/archive.html') « Arquivos
    if nextPage
      a(href=nextPage.url) Próxima Página »



